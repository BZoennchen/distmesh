use distmesh::Point;
use distmesh::geometry::equiliteral_triangle;
use distmesh::prelude::*;

pub fn main() {
  /*let (p1, p2, p3) = equiliteral_triangle(100.0);
  let mesh = Mesh::triangle(p1, p2, p3);
  println!("{}", distmesh::mesh::tikz::to_tikz_string(&mesh));*/
}